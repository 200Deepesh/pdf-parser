/**
 * Add part-of-speech (POS) tags.
 *
 * @returns
 *   Transform.
 */
export default function retextPos(): (tree: Root) => undefined;
export type Root = import('nlcst').Root;
export type Word = import('nlcst').Word;
